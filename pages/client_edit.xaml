<Page x:Class="barbi_shop.pages.client_edit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:barbi_shop.pages"
      mc:Ignorable="d" 
      d:DesignHeight="95" d:DesignWidth="667"
      Title="client_edit">

    <Grid Background="white">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Grid.Column="0" Header="Новый клиент">
            <Button  Content="Добавить" Click="Button_Click" ></Button>
        </GroupBox>
        <GroupBox  Grid.Row="0" Grid.Column="1" Header="Изменение клиента">
            <Button  Content="Изменить" Name="change_client_btn" Click="change_client_btn_Click"></Button>
        </GroupBox>


    </Grid>
</Page>