<Page x:Class="barbi_shop.pages.service_list"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:barbi_shop.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="service_list">


    <Grid Background="white">
        <ScrollViewer>
            <ListView Name="service_view_list" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding service_id}"/>
                        <GridViewColumn Header="Наименование" DisplayMemberBinding="{Binding service_name}"/>
                        <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding service_price}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>
